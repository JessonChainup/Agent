# Discovery 笔记（可滚动补充）

<!-- Gate：SPEC 前应至少有「现状 + 竞品/先例」占位；无竞品可写内部替代方案 -->

## 现状

- 方法论资产在 **`product-delivery/`** ↔ 服务器 **`references/product-delivery/`**；
- 生产交付指向 **[JessonChainup/Agent](https://github.com/JessonChainup/Agent)**；
- MCP 草稿目录约定：`~/chainup-pm-workspace/drafts/<REQ-ID>/`（镜像阶段结构）。

## 竞品 / 参照（可选）

- GitHub Actions + Markdown 需求仓（行业通行）；
- 内部：Hermes **`chainup-fs`** 能力与 **Gate**（见 **`ORCHESTRATION.md`**）。

## 待定问题

1. 主产品线「真实」仓库与本交付仓的双向同步策略：**TODO**。  
2. 是否必须为每条 REQ 开独立 branch：**待定**。
